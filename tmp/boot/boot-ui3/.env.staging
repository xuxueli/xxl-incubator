# 系统名称
VITE_APP_TITLE=Boot管理系统

# 环境配置
VITE_APP_ENV='staging'

# 前端端口号
VITE_APP_PORT=3000

# 后端API地址
VITE_API_URL=http://localhost:8080
# 后端API前缀
VITE_APP_BASE_API='/stage-api'

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip