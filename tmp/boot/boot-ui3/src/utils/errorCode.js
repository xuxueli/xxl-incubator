/**
 * 错误码映射表模块（errorCode.js）
 *
 * 职责：
 *   - 集中维护后端业务响应码与前端展示文案的映射关系。
 *   - 供请求拦截器（request.js）使用，将后端 code 转换为用户友好的提示信息。
 *   - 新增/修改错误码提示时，只需在此文件维护，无需改动请求层逻辑。
 *
 * 键名说明：
 *   - 数字字符串键：对应 HTTP 状态码或后端业务码。
 *   - 'default'：兜底提示，当 code 无匹配映射时使用。
 */
export default {
  '401': '认证失败，无法访问系统资源',
  '403': '当前操作没有权限',
  '404': '访问资源不存在',
  'default': '系统未知错误，请反馈给管理员'
}
